# Root kustomization: use overlays per environment.
# Deploy with: kustomize build overlays/dev | kubectl apply -f -
# Or via Argo CD ApplicationSet nfl-wallet (multiple sources).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []
